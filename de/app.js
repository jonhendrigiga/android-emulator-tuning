function plan(){var ram=+document.getElementById('ram').value,core=+document.getElementById('core').value,gpu=+document.getElementById('gpu').value,use=document.getElementById('use').value;
var open={8:'3-4',16:'6-8',32:'10-12',64:'15+'}[ram];var perCore=use==='multi'?2:Math.max(4,Math.floor(core/2));var perMem=use==='multi'?'2048 MB':Math.min(8192,ram*1024*0.5)+' MB';var fps=use==='multi'?'30 fest':'60 fest (120 bei schnellem Display)';var render=gpu?'DirectX/Vulkan (dediziert)':'OpenGL (integriert)';var warn=gpu?'':'Ohne dedizierte GPU lieber Einzelinstanz.';
document.getElementById('out').style.display='block';document.getElementById('out').innerHTML='<div class=rec>Empfohlen: '+(use==='multi'?open+' Instanzen':'1 (Qualität)')+'</div><p>Pro Instanz: CPU '+perCore+' Kerne · RAM '+perMem+' · '+fps+' · '+render+'</p><p><small>VT aktivieren (+50-100%); RAM gesamt <=50%; SSD; '+warn+'</small></p>';}