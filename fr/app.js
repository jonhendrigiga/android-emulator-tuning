function plan(){var ram=+document.getElementById('ram').value,core=+document.getElementById('core').value,gpu=+document.getElementById('gpu').value,use=document.getElementById('use').value;
var open={8:'3-4',16:'6-8',32:'10-12',64:'15+'}[ram];var perCore=use==='multi'?2:Math.max(4,Math.floor(core/2));var perMem=use==='multi'?'2048 Mo':Math.min(8192,ram*1024*0.5)+' Mo';var fps=use==='multi'?'verrou 30':'verrou 60 (120 si écran rapide)';var render=gpu?'DirectX/Vulkan (dédié)':'OpenGL (intégré)';var warn=gpu?'':'Sans GPU dédié, préférez une seule instance.';
document.getElementById('out').style.display='block';document.getElementById('out').innerHTML='<div class=rec>Suggéré : '+(use==='multi'?open+' instances':'1 (qualité)')+'</div><p>Par instance : CPU '+perCore+' cœurs · RAM '+perMem+' · '+fps+' · '+render+'</p><p><small>Activez le VT (+50-100%) ; RAM totale <=50% ; SSD ; '+warn+'</small></p>';}