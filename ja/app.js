function plan(){var ram=+document.getElementById('ram').value,core=+document.getElementById('core').value,gpu=+document.getElementById('gpu').value,use=document.getElementById('use').value;
var open={8:'3-4',16:'6-8',32:'10-12',64:'15+'}[ram];var perCore=use==='multi'?2:Math.max(4,Math.floor(core/2));var perMem=use==='multi'?'2048MB':Math.min(8192,ram*1024*0.5)+'MB';var fps=use==='multi'?'30固定':'60固定(高リフレッシュは120)';var render=gpu?'DirectX/Vulkan(専用)':'OpenGL(内蔵)';var warn=gpu?'':'専用GPU無しのマルチは重い。単体推奨。';
document.getElementById('out').style.display='block';document.getElementById('out').innerHTML='<div class=rec>推奨: '+(use==='multi'?open+' 台':'1台(画質優先)')+'</div><p>1台あたり: CPU '+perCore+'コア · RAM '+perMem+' · '+fps+' · '+render+'</p><p><small>必ずVT有効化(+50-100%)；合計RAMは物理の50%以下；SSD使用；'+warn+'</small></p>';}